environment: aws-scenario-hpa

deployment:
  replicas: 2
  podLabels:
    medikong.io/scenario: "true"
    medikong.io/scenario-scope: hpa

hpa:
  enabled: true
  minReplicas: 2
  maxReplicas: 4
  targetCPUUtilizationPercentage: 60
